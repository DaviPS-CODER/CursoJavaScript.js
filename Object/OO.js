//código não executável!!

//procedural ( procedimento) = um trecho que é executado em outras partes
processament(dado1,dado2)

//OO
 /*objeto*/{
    valor1, //atributos
    valor2,
    processamento // método
}
Objeto.processamento //os valores e métodos estão numa "capsula" que seria o objeto

//Abstração:
//Consiste em esconder os  detalhes desnecessários de um determinado processo ou objeto.
//focar apenas nos elementos essenciais daquele objeto

//encapsulamento:
//é  basicamento mostrar apenas o necessário, os detalhes da operação permanecem escondidos.
// e caso precise fazer uma alteração, vc altera apenas oq está escondido, e n a interface tbm
// é importante para gerar codependencia entre os objetos, em não expor todos os detalhes que cada um deles tem

//Herança (prototype):
//basicamente é gerar objetos mais complexos a partir de outros mais simples
//no objeto carro vc tem o motor, mas a complexidade do objeto motor está em outra parte.
// e assim vc n precisa de colocar toda a complexidade de "motor" dentro de "carro".
// vc reusa atributos e métodos de um objeto "pai".

//POLIMORFISMO:
// vc traça uma ideia, e apartir dessa ideia, objetos passam a ser construidos.
// é um conceito que assume multiplas formas.
//a partir de uma variavel de uma natureza mais genérica, que servem de modelo para objetos mais concretos.